import { withStyles } from '@material-ui/core/styles'
import CustomButton from '../customButton/CustomButton'

const styles = {
  root: {
    margin: '1.125rem 20px',
  },
}

export default withStyles(styles)(CustomButton)
